kotlin {
    linuxArm32Hfp('zero') {
        binaries {
            executable("gpio") {
                entryPoint 'uk.tvidal.kpi.gpio.main'
            }
        }
    }
}

dependencies {
    zeroImplementation project(':zero')
}
